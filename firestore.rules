rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Default allow read, write for system collections in AndradeAgro app
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
